# Figura Offline Fix (1.2.5) 🛠️

Специальный аддон для мода **Figura**, который исправляет работу аватаров на пиратских лаунчерах (Offline mode) и серверах с использованием системы скинов **Ely.by**.

## ✨ Основные функции
- **Обход авторизации:** Позволяет моду думать, что вы авторизованы, даже если у вас нет лицензии Mojang.
- **Offline Support:** Исправляет методы `isOnlineMode` и `isProfileVerified`, открывая доступ к функционалу аватаров.
- **Кэширование аватаров:** Сохраняет загруженные аватары в папку `figura_offline_cache`. Если бэкенд Figura упадет или будет недоступен, ваш аватар всё равно загрузится из локальной копии.
## 📜 Команды (Доступны только вам)

- `/figura-fix reload` — Перезагрузить аватар (сообщения приходят в ЛС).
- `/figura-fix debug` — Посмотреть детальную информацию о работе мода (только для вас).

## 🧱 Совместимость
Аддон разработан с учетом максимальной совместимости и **не конфликтует** с:
- **Figura v5 Support** (поддержка нового формата аватаров).
- **Figura Extra Bone** (дополнительные кости моделей).

## 🚀 Установка
1. Скачайте готовую версию из [Releases](https://github.com/MrModel228/Figura-mod-fix/releases).
2. Положите файл в папку `mods` вместе с оригинальным модом Figura.
3. Запустите игру.

## 🛠️ Сборка из исходников
Если вы хотите собрать мод самостоятельно:
1. Убедитесь, что оригинальный JAR-файл Figura (версии 0.1.5 для 1.21.8) находится в папке `libs/`.
2. Выполните команду в терминале:
   ```bash
   ./gradlew clean build